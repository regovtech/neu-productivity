{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    {
      "path": "/api/cron/gdpr-hard-delete",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/send-digest",
      "schedule": "0 * * * 1"
    }
  ]
}
